import { DefaultTheme } from "styled-components";
import color from "../styles/colorStyleLight";
import layout from "../styles/layoutStyle";

const theme: DefaultTheme = {
  color,
  layout,
};

export default theme;
